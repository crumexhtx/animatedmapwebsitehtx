/**
 * Placeholder: refresh ACS population / income / housing fields from the Census API.
 * Set CENSUS_API_KEY and re-run once live ingestion is wired.
 */

console.log(
  'enrich-census: not implemented yet. Launch catalog uses curated JSON in data/raw/cities-seed.json.',
)
console.log('When ready: read cities, call ACS endpoints, write data/raw/enrichments/census.json patches.')
process.exit(0)
